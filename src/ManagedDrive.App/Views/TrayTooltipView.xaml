<UserControl x:Class="ManagedDrive.App.Views.TrayTooltipView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource AppSurface}"
            BorderBrush="{DynamicResource AppDivider}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12,10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12"
                              Direction="270"
                              Opacity="0.15"
                              ShadowDepth="4" />
        </Border.Effect>

        <Grid>
            <ItemsControl x:Name="DiskList"
                          ItemsSource="{Binding Disks}">
                <ItemsControl.Style>
                    <Style TargetType="ItemsControl">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ItemsControl.Style>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="36" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       FontSize="13"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource AppPrimary}"
                                       Text="{Binding MountPoint}" />
                            <TextBlock Grid.Column="1"
                                       FontSize="13"
                                       Foreground="{DynamicResource AppForeground}"
                                       Text="{Binding VolumeLabel}"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Grid.Column="2"
                                       FontSize="13"
                                       Foreground="{DynamicResource AppForegroundLight}"
                                       Text="{Binding UsedPercent, StringFormat={}{0:F0}%}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock FontSize="13"
                       Foreground="{DynamicResource AppForegroundLight}"
                       Text="{DynamicResource Tray.NoDisksMounted}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=DiskList, Path=HasItems}" Value="False">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
    </Border>
</UserControl>
