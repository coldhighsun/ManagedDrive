<Window x:Class="ManagedDrive.App.Views.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Style="{StaticResource AppWindow}"
        Width="400" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Icon + title -->
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock FontFamily="Segoe Fluent Icons" FontSize="24"
                       Text="&#xE7BA;"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource AppWarning}"/>
            <TextBlock x:Name="TitleText"
                       VerticalAlignment="Center"
                       Margin="10,0,0,0"
                       FontSize="16" FontWeight="Medium"/>
        </StackPanel>

        <!-- Body text -->
        <TextBlock x:Name="BodyText"
                   Grid.Row="2"
                   TextWrapping="Wrap"
                   LineHeight="20"
                   Foreground="{DynamicResource AppForegroundLight}"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{DynamicResource Btn.Cancel}" Width="80" IsCancel="True"
                    Style="{StaticResource FlatButton}"
                    Margin="0,0,8,0"/>
            <Button Content="{DynamicResource Btn.Ok}" Width="80" IsDefault="True"
                    Click="OkButton_Click"
                    Style="{StaticResource RaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>
